#include <stdio.h>
/**
 * main- the main method, where all the codes are checked and the entry point of code
 * return- it will always return 0 is it is successful(erro free)
 */
int main(void)
{
	puts("Programming is like building a multilingual puzzle\n");
	return (0);
}
