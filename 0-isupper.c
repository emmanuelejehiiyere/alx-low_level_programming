#include "main.h"
/**
 * _isupper - Function for task
 * @c: Check if c is uppercase
 * Return: 1 if uppercase
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
