#include "main.h"
/**
 * set_string - sets the value of a pointer to a char
 * @to: parameter type char
 * @s: parameter type char
 * Return: Always 0 if Successful
 */
void set_string(char **s, char *to)
{
	*s = to;
}
