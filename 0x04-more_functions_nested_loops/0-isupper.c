#include "main.h"
/**
 * _isupper - Function for task
 *@c: Check for upperCase
 *Return: 1 if upper case
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else
		return (0);
}
